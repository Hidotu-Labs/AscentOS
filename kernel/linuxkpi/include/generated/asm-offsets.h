/* Stand-in for Kbuild's generated assembler offsets.  No imported code in
 * Phase 0 needs real offsets; later phases can replace this with generated
 * values if a header actually consumes OFFSET()/DEFINE() output. */
#ifndef __AVORY_LINUXKPI_ASM_OFFSETS_H
#define __AVORY_LINUXKPI_ASM_OFFSETS_H
#endif
